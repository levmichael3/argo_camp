apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: jenkins-ing
spec:
  rules:
    -  host: jenkins-dev.blackowl.click
       http:
        paths:
          - backend:
              serviceName: jenkins-operator-http-jenkins
              servicePort: 8080
            path: '/'
            pathType: Prefix
